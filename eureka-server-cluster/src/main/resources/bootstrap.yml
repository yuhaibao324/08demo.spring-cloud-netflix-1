#spring:
#  cloud:
#    config:
#      name: eureka-peer2
#      uri: http://localhost:3600

spring:
  cloud:
    config:
      name: eureka-peer2
      discovery:
        enabled: true
        serviceId: config-service